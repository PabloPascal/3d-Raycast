#include <SFML/Audio.hpp>
#include <cmath>
#include <iostream>
#include <string>
#include "../tests/test.cpp"


int main(int argc, char* argv[]) {

	testEngine(argv[0]);

	// std::map<int, sf::Sound> s;
	// sf::SoundBuffer buffer;
	// buffer.loadFromFile("res/Shotgun_fire.wav");
	// sf::Sound sound(buffer);
	// s.insert(std::make_pair(1, sound));

	// while(1){

	// 	if(sf::Mouse::isButtonPressed(sf::Mouse::Left)){
	// 		s[1].play();
	// 	}

	// }


	return 0;

}

